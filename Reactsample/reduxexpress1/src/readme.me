/**
 * <input type="number" name="fetchDoctorById" ref={IdRef}/>
 * 
 * // inputRef={hospitalIdRef}
 * WHEN USING from "@mui/material" use 'inputRef' instead of 'ref'
 * while react getboostrap uses just ref
 * <TextField id="outlined-basic"  name="fetchDoctorByName" label="Enter Name to fetch" variant="outlined" type="text"  inputRef={hospitalIdRef}/>     
 */

 